export interface ChartAxisProps {
  orientation?: 'x' | 'y';
  ticks?: number;
  domain?: [number, number];
  range?: [number, number];
}

export function ChartAxis(props: ChartAxisProps) {
  void props;
  return null;
}
